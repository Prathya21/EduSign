# OpenAI API Configuration for Whisper Speech-to-Text
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Default language for Whisper (e.g., en, hi, gu)
# WHISPER_DEFAULT_LANGUAGE=en

# Optional: Enable mock mode for local testing without API key (true/false)
# WHISPER_MOCK_FALLBACK=true
